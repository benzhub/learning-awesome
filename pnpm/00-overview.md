# pnpm 學習地圖

## 什麼是 pnpm？

pnpm（performant npm）是一個快速、節省磁碟空間的 Node.js 套件管理工具。
相較於 npm 和 yarn，它透過 **硬連結（hard link）+ 符號連結（symlink）** 管理 `node_modules`，
讓所有專案共用同一份套件快取，大幅減少重複下載與磁碟佔用。

## 為什麼選 pnpm？

| 特性 | npm | yarn | pnpm |
|------|-----|------|------|
| 安裝速度 | 普通 | 快 | **最快** |
| 磁碟空間 | 多 | 多 | **最省** |
| node_modules 結構 | 平鋪（混亂） | 平鋪 | **嚴格隔離** |
| Monorepo 支援 | 弱 | 支援 | **原生支援** |
| 幽靈依賴問題 | 有 | 有 | **無** |

> 💡 **幽靈依賴（Ghost Dependency）**：npm 平鋪結構讓你可以使用未宣告在 `package.json` 的套件，pnpm 嚴格結構杜絕此問題。

## 核心機制

```
~/.pnpm-store/               ← 全域內容可尋址儲存（只存一份）
    └── v3/
        └── files/
            └── <hash>/...

專案/node_modules/
    ├── .pnpm/               ← 所有套件的硬連結（hard link）
    │   └── react@18.0.0/
    └── react -> .pnpm/...  ← 符號連結（symlink）
```

## 學習地圖

```
📁 pnpm/
├── 00-overview.md        ← 你在這裡
├── 01-quick-start.md     → 安裝 pnpm、初始化專案、基礎指令
├── 02-core-concepts.md   → store、symlink、嚴格依賴結構
├── 03-common-patterns.md → 日常開發指令大全
├── 04-advanced.md        → Workspace Monorepo 架構
└── 05-best-practices.md  → 最佳實踐與避坑指南
```

## 建議學習順序

1. **完全新手** → 01 → 03 → 05
2. **有 npm/yarn 經驗** → 01 快速掃過 → 02 理解差異 → 04 Monorepo
3. **管理 Monorepo** → 02 → 04 → 05

---

> 下一步：[01-quick-start.md](./01-quick-start.md) — 10 分鐘跑通第一個範例
