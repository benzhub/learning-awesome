# 03 — 對話中的斜線指令

## 什麼是斜線指令？

在互動模式對話框裡輸入 `/` 開頭的指令，可以控制 Claude Code 的行為，不需要退出重開。

---

## 最常用的斜線指令

### `/help`
列出所有可用的斜線指令。

```
> /help
```

---

### `/clear`
清除目前的對話記憶，從頭開始（但不退出程式）。

```
> /clear
```

> 💡 對話很長、AI 開始「忘事」或答非所問時用這個重置。

---

### `/compact`
壓縮對話歷史，騰出更多 context 空間，繼續作業不中斷。

```
> /compact
```

對話很長但還不想 `/clear` 時使用。

---

### `/model`
切換使用的模型，不用退出重來。

```
> /model
```

會列出可用模型讓你選。

---

### `/exit`
退出 Claude Code CLI。

```
> /exit
```

---

## 進階斜線指令

| 指令 | 說明 |
|------|------|
| `/status` | 查看目前 context 使用量 |
| `/config` | 查看或修改設定 |
| `/init` | 分析目前專案，產生 `CLAUDE.md` |
| `/review` | 請 AI 審查目前的 git diff |
| `/memory` | 查看 AI 目前的記憶內容 |

---

## 小技巧

**`/init` 讓 AI 先了解你的專案**

第一次在新專案用 Claude Code 時，先跑：

```
> /init
```

AI 會掃描整個專案結構，自動產生 `CLAUDE.md`，之後每次啟動都會讀這份文件，不用每次重新介紹專案。

---

## 小結

| 指令 | 時機 |
|------|------|
| `/help` | 忘記有什麼指令時 |
| `/clear` | AI 開始答非所問，重置對話 |
| `/compact` | 對話很長但想繼續 |
| `/model` | 換模型不用退出 |
| `/init` | 新專案第一次用，產生 CLAUDE.md |
| `/exit` | 離開 |

---

> 下一步：`04-non-interactive.md` — 用非互動模式跑腳本和 CI/CD
