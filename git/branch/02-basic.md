# Git Branch 基本操作

## 建立分支

### 只建立、不切換

```bash
git branch <分支名稱>
```

### 建立並切換（最常用）

```bash
git checkout -b <分支名稱>
```

或使用較新的 `switch` 指令：

```bash
git switch -c <分支名稱>
```

### 從指定 commit 建立

```bash
git checkout -b <分支名稱> <commit-hash>
```

---

## 切換分支

```bash
git checkout <分支名稱>
# 或
git switch <分支名稱>
```

> ⚠️ 切換前若有未 commit 的改動，需先 `stash` 或 commit。

---

## 列出分支

```bash
# 列出本地分支
git branch

# 列出遠端分支
git branch -r

# 列出所有分支（本地 + 遠端）
git branch -a

# 顯示最後一次 commit 資訊
git branch -v
```

---

## 刪除分支

```bash
# 刪除已合併的本地分支
git branch -d <分支名稱>

# 強制刪除（未合併也會刪）
git branch -D <分支名稱>

# 刪除遠端分支
git push origin --delete <分支名稱>
```

---

## 重新命名分支

```bash
# 重新命名目前所在分支
git branch -m <新名稱>

# 重新命名指定分支
git branch -m <舊名稱> <新名稱>
```

---

## 快速記憶

| 指令 | 說明 |
|------|------|
| `git branch` | 列出本地分支 |
| `git branch <名稱>` | 建立分支 |
| `git checkout -b <名稱>` | 建立並切換 |
| `git checkout <名稱>` | 切換分支 |
| `git branch -d <名稱>` | 刪除已合併分支 |
| `git branch -D <名稱>` | 強制刪除分支 |

---

## 下一步

👉 [03-naming.md](./03-naming.md) — 分支命名規範
